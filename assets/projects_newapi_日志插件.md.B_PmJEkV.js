import{_ as o,o as e,c as s,a2 as n}from"./chunks/framework.COr20JJv.js";const p=JSON.parse('{"title":"日志插件（usage_log）业务说明","description":"ext/usage_log 业务目标、配置、附件、API 与 excelize 依赖说明","frontmatter":{"outline":"deep","description":"ext/usage_log 业务目标、配置、附件、API 与 excelize 依赖说明"},"headers":[],"relativePath":"projects/newapi/日志插件.md","filePath":"projects/newapi/日志插件.md"}'),d={name:"projects/newapi/日志插件.md"};function r(a,t,i,g,c,l){return e(),s("div",null,[...t[0]||(t[0]=[n(`<h1 id="日志插件-usage-log-业务说明" tabindex="-1">日志插件（usage_log）业务说明 <a class="header-anchor" href="#日志插件-usage-log-业务说明" aria-label="Permalink to &quot;日志插件（usage_log）业务说明&quot;">​</a></h1><h2 id="文档地图-usage-log-相关" tabindex="-1">文档地图（usage_log 相关） <a class="header-anchor" href="#文档地图-usage-log-相关" aria-label="Permalink to &quot;文档地图（usage_log 相关）&quot;">​</a></h2><table tabindex="0"><thead><tr><th>文档</th><th>读它当你需要…</th></tr></thead><tbody><tr><td><strong>本文</strong></td><td><strong>业务</strong>：目标、流程、环境变量、附件、API、前端展示、限制。</td></tr><tr><td><strong><a href="./插件开发.html">插件开发.md</a></strong></td><td><strong>机制</strong>：<code>ext</code> 包、目录树、新建插件、表初始化、联调、附录。</td></tr><tr><td><strong><a href="./usage_log与上游合并-源码入侵.html">usage_log与上游合并-源码入侵.md</a></strong></td><td><strong>合并</strong>：主仓库非 <code>ext</code> 须保留的代码与 i18n。</td></tr><tr><td><strong><a href="./与上游-Gitee-手动合并步骤.html">与上游-Gitee-手动合并步骤.md</a></strong></td><td><strong>Git</strong>：与 Gitee 无共同祖先时的合并命令。</td></tr></tbody></table><p><strong>本文定位：</strong> <strong><code>ext/usage_log</code> 做什么、怎么配</strong>（以本文为准）；不写合并用的逐文件代码块（见上表第三列）。</p><hr><h2 id="概述" tabindex="-1">概述 <a class="header-anchor" href="#概述" aria-label="Permalink to &quot;概述&quot;">​</a></h2><p><code>ext/usage_log</code> 在网关中的主要作用，是把与单次中继请求相关的<strong>可读摘要</strong>写入独立表 <strong><code>usage_log_content</code></strong>，并通过 <strong><code>request_id</code></strong> 与主库 <strong><code>logs</code></strong> 关联，供管理端查询「使用日志」。</p><p>落库字段通常包括：<strong>系统提示词摘要</strong>、<strong>用户问题</strong>、<strong>模型回答摘要</strong>（具体解析与截断见下文）。<strong>不修改</strong>上游 <code>logs</code> / <code>model.Log</code> 等已有表结构；计费与主日志仍走原有链路。</p><h3 id="本文档结构-与后文编号小节对应" tabindex="-1">本文档结构（与后文编号小节对应） <a class="header-anchor" href="#本文档结构-与后文编号小节对应" aria-label="Permalink to &quot;本文档结构（与后文编号小节对应）&quot;">​</a></h3><table tabindex="0"><thead><tr><th>#</th><th>主题</th><th>说明</th></tr></thead><tbody><tr><td>1</td><td><strong>目标与边界</strong></td><td>维护扩展日志、不改变主库表结构。</td></tr><tr><td>2</td><td><strong>接入顺序（与 Gin 行为相关）</strong></td><td>Gin 中间件注册顺序及对 capture 路径的影响。</td></tr><tr><td>3</td><td><strong>业务主流程（一次请求）</strong></td><td>从读 body 到写扩展表、关联附件的端到端步骤。</td></tr><tr><td>4–5</td><td><strong>请求侧逻辑</strong> / <strong>响应侧逻辑</strong></td><td>捕获路径、<code>request_id</code>、解析规则与流式/非流式响应提取。</td></tr><tr><td>6</td><td><strong>落库规则</strong></td><td>以 <code>request_id</code> 为键的 upsert 等行为。</td></tr><tr><td>7</td><td><strong>长度与截断（业务语义）</strong></td><td>落库前按配置截断长文本。</td></tr><tr><td>8</td><td><strong>配置项汇总（环境变量）</strong></td><td>环境变量与业务含义对照。</td></tr><tr><td>9</td><td><strong>附件（消息内链接 + 预上传文件）</strong></td><td>消息内链接、multipart 预上传与注入等；<strong>9.6</strong> 说明 <strong>Excel 表格</strong>与主仓 <strong><code>go.mod</code> 中 excelize</strong> 的作用。</td></tr><tr><td>10</td><td><strong>只读 API</strong></td><td>扩展提供的查询接口。</td></tr><tr><td>11</td><td><strong>前端（可选扩展）</strong></td><td>管理端 Web 展示（扩展组件）。</td></tr><tr><td>12</td><td><strong>限制与已知情况</strong></td><td>使用边界与已知问题。</td></tr><tr><td>13</td><td><strong>源码索引</strong></td><td>关键源码路径速查。</td></tr><tr><td>14</td><td><strong>四份文档怎样配合</strong></td><td>与 <a href="./插件开发.html">插件开发</a>、<a href="./usage_log与上游合并-源码入侵.html">源码侵入</a>、<a href="./与上游-Gitee-手动合并步骤.html">Gitee 合并</a> 的分工。</td></tr></tbody></table><p>本文旨在细化 <strong>usage_log</strong> 的用途与实现细节，并与《<a href="./插件开发.html">插件开发.md</a>》形成互补。</p><hr><h2 id="_1-目标与边界" tabindex="-1">1. 目标与边界 <a class="header-anchor" href="#_1-目标与边界" aria-label="Permalink to &quot;1. 目标与边界&quot;">​</a></h2><table tabindex="0"><thead><tr><th>项目</th><th>说明</th></tr></thead><tbody><tr><td><strong>目标</strong></td><td>在独立表 <strong><code>usage_log_content</code></strong> 中，按 <strong><code>request_id</code></strong> 与主库 <strong><code>logs</code></strong> 关联，额外保存 <strong>system 提示词摘要</strong>、<strong>用户问题</strong>、<strong>模型回答摘要</strong>，供管理端「使用日志」展示或接口查询。</td></tr><tr><td><strong>不修改</strong></td><td>不改动上游 <strong><code>model.Log</code> / <code>logs</code> 表结构</strong>；计费、主日志写入逻辑仍由网关原有链路完成。</td></tr><tr><td><strong>数据库</strong></td><td>读写使用 <strong><code>model.LOG_DB</code></strong>（与主日志库一致）；表由注册时 <strong><code>AutoMigrate</code></strong> 或 <code>ext/table_init</code> 脚本初始化。</td></tr></tbody></table><hr><h2 id="_2-接入顺序-与-gin-行为相关" tabindex="-1">2. 接入顺序（与 Gin 行为相关） <a class="header-anchor" href="#_2-接入顺序-与-gin-行为相关" aria-label="Permalink to &quot;2. 接入顺序（与 Gin 行为相关）&quot;">​</a></h2><p>要点：<strong><code>ext.MustRegister</code> 须在 <code>router.SetRouter</code> 之前</strong>；<strong><code>ext.RegisterRelayMiddleware</code> 须在 relay 侧解压之后、第一个 <code>/v1</code> 等子路由 <code>Group</code> 之前</strong>——否则捕获链进不了聊天接口。Session 须在 <code>MustRegister</code> 之前挂载（详见 <a href="./插件开发.html#sec-9-1">插件开发 · 第 9.1 节</a>）。</p><p><strong>可复制的代码块</strong>（含 <code>middleware/auth.go</code> 的 <code>EnforceUserAuth</code>、<code>go.mod</code> 等）见 <a href="./usage_log与上游合并-源码入侵.html#backend-go">usage_log与上游合并-源码入侵 · 后端 Go</a> 与 <strong>非 ext 文件一览</strong>；合并冲突时以该文 <strong>〔合并段〕</strong> 为准。</p><hr><h2 id="_3-业务主流程-一次请求" tabindex="-1">3. 业务主流程（一次请求） <a class="header-anchor" href="#_3-业务主流程-一次请求" aria-label="Permalink to &quot;3. 业务主流程（一次请求）&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>请求进入 CaptureMiddleware</span></span>
<span class="line"><span>  → 总开关关闭？ → 直接 Next()</span></span>
<span class="line"><span>  → 非 POST 或非 capture 路径？ → 直接 Next()</span></span>
<span class="line"><span>  → 无 request_id（common.RequestIdKey）？ → 直接 Next()</span></span>
<span class="line"><span>  → 读请求体，解析 system / prompt / stream（见第 4 节）</span></span>
<span class="line"><span>  → 还原 BodyStorage，与 common.KeyBodyStorage 对齐，供下游 relay 复用</span></span>
<span class="line"><span>  → 包装 ResponseWriter，执行 c.Next()（relay 处理）</span></span>
<span class="line"><span>  → c.Next() 后：若存在「有效 system」（KeyEffectiveSystemPrompt），覆盖 system 文本（见第 4.4 节）</span></span>
<span class="line"><span>  → 非流式：从完整响应 JSON 提取回答；流式：从缓冲的 SSE 拼接回答（见第 5 节）</span></span>
<span class="line"><span>  → 按 MaxPromptLen / MaxCompletionLen 截断（见第 7 节）</span></span>
<span class="line"><span>  → SaveContent：按 request_id upsert 到 usage_log_content</span></span>
<span class="line"><span>  → processAttachmentsFromBody：关联 usage_log_attachment_tokens、写入消息内 URL 附件</span></span></code></pre></div><hr><h2 id="_4-请求侧逻辑" tabindex="-1">4. 请求侧逻辑 <a class="header-anchor" href="#_4-请求侧逻辑" aria-label="Permalink to &quot;4. 请求侧逻辑&quot;">​</a></h2><h3 id="_4-1-捕获路径-仅-post" tabindex="-1">4.1 捕获路径（仅 POST） <a class="header-anchor" href="#_4-1-捕获路径-仅-post" aria-label="Permalink to &quot;4.1 捕获路径（仅 POST）&quot;">​</a></h3><p>当前固定为以下路径（与 <code>IsCapturePath</code> 一致）：</p><ul><li><code>/v1/chat/completions</code></li><li><code>/v1/completions</code></li><li><code>/v1/messages</code></li><li><code>/pg/chat/completions</code></li></ul><h3 id="_4-2-依赖-request-id" tabindex="-1">4.2 依赖 <code>request_id</code> <a class="header-anchor" href="#_4-2-依赖-request-id" aria-label="Permalink to &quot;4.2 依赖 \`request_id\`&quot;">​</a></h3><p>中间件从 Gin Context 读取 <strong><code>common.RequestIdKey</code></strong>（与响应头 <strong><code>X-Oneapi-Request-Id</code></strong>、使用日志表 <strong><code>logs.request_id</code></strong> 一致）。若为空则<strong>不捕获</strong>、不写扩展表。</p><h3 id="_4-3-请求体解析" tabindex="-1">4.3 请求体解析 <a class="header-anchor" href="#_4-3-请求体解析" aria-label="Permalink to &quot;4.3 请求体解析&quot;">​</a></h3><table tabindex="0"><thead><tr><th>字段</th><th>含义</th><th>说明</th></tr></thead><tbody><tr><td><strong>system</strong></td><td><code>messages</code> 中 <code>role=system</code> 的 <code>content</code> 拼接</td><td>多段 system 合并为多行文本；与 <strong>MaxPromptLen</strong> 截断后落 <strong><code>system_prompt_text</code></strong>。</td></tr><tr><td><strong>prompt / 问题</strong></td><td>非 system 的 <code>messages</code> 的 <code>content</code>，或旧版 <code>prompt</code> 字段</td><td>支持字符串与多模态数组中的 <code>type:text</code>；截断后落 <strong><code>prompt_text</code></strong>。</td></tr><tr><td><strong>stream</strong></td><td>是否流式</td><td>除标准 <code>true</code>/<code>false</code> 外，兼容字符串 <code>&quot;true&quot;</code>/<code>&quot;false&quot;</code>、<code>&quot;1&quot;</code>/<code>&quot;0&quot;</code> 等，避免误判分支。</td></tr></tbody></table><h3 id="_4-4-有效-system-提示词-渠道注入" tabindex="-1">4.4 有效 system 提示词（渠道注入） <a class="header-anchor" href="#_4-4-有效-system-提示词-渠道注入" aria-label="Permalink to &quot;4.4 有效 system 提示词（渠道注入）&quot;">​</a></h3><p>Relay 可能在处理过程中写入<strong>实际生效</strong>的 system 文本。中间件在 <strong><code>c.Next()</code> 之后</strong> 若读到 <strong><code>KeyEffectiveSystemPrompt</code></strong>（定义在 <code>ext/usage_log/keys.go</code>），则<strong>用其替换</strong>此前从请求体解析的 system 摘要，再截断落库。</p><hr><h2 id="_5-响应侧逻辑" tabindex="-1">5. 响应侧逻辑 <a class="header-anchor" href="#_5-响应侧逻辑" aria-label="Permalink to &quot;5. 响应侧逻辑&quot;">​</a></h2><h3 id="_5-1-非流式-stream-为-false" tabindex="-1">5.1 非流式（<code>stream</code> 为 false） <a class="header-anchor" href="#_5-1-非流式-stream-为-false" aria-label="Permalink to &quot;5.1 非流式（\`stream\` 为 false）&quot;">​</a></h3><ul><li>使用 <strong><code>responseCaptureWriter</code></strong> 缓存 <strong><code>Write</code> / <code>WriteString</code></strong> 输出，并转发 <strong><code>Flush</code></strong>（保证 SSE 类实现不丢流）。</li><li>响应体按 <strong>OpenAI 风格 JSON</strong> 解析回答，支持： <ul><li>Chat：<code>choices[0].message</code> 的 <code>content</code>，以及 <code>reasoning_content</code> / <code>reasoning</code>（与 <code>content</code> 合并）；</li><li>旧版补全：<code>choices[0].text</code>。</li></ul></li></ul><h3 id="_5-2-流式-stream-为-true" tabindex="-1">5.2 流式（<code>stream</code> 为 true） <a class="header-anchor" href="#_5-2-流式-stream-为-true" aria-label="Permalink to &quot;5.2 流式（\`stream\` 为 true）&quot;">​</a></h3><ul><li>同样缓存完整 SSE 字节流，结束后按 <strong><code>data:</code></strong> 行解析 JSON。</li><li>从每条 <strong><code>choices[0].delta</code></strong> 中拼接文本，字段包括 <strong><code>reasoning_content</code>、<code>reasoning</code>、<code>content</code>、<code>text</code></strong>（兼容推理模型与旧补全流）。</li><li><strong><code>RecordStreamCompletion</code></strong> 为 <strong>false</strong> 时：仍写 <strong>system / 问题</strong>，<strong>不写</strong> <code>completion_text</code>（见配置表）。</li></ul><h3 id="_5-3-与上游失败的关系" tabindex="-1">5.3 与上游失败的关系 <a class="header-anchor" href="#_5-3-与上游失败的关系" aria-label="Permalink to &quot;5.3 与上游失败的关系&quot;">​</a></h3><p>若 relay 未返回 200 或 body 非预期，则<strong>可能得到空回答</strong>；扩展仍可能写入 prompt 侧，<strong>completion 为空</strong>。</p><hr><h2 id="_6-落库规则" tabindex="-1">6. 落库规则 <a class="header-anchor" href="#_6-落库规则" aria-label="Permalink to &quot;6. 落库规则&quot;">​</a></h2><table tabindex="0"><thead><tr><th>规则</th><th>说明</th></tr></thead><tbody><tr><td><strong>表</strong></td><td><code>usage_log_content</code></td></tr><tr><td><strong>唯一键</strong></td><td><code>request_id</code></td></tr><tr><td><strong>写入方式</strong></td><td><code>SaveContent</code>：<strong>单条 upsert</strong>（冲突则更新正文与时间戳）；<strong><code>CreatedAt</code></strong> 在更新时也会刷新。</td></tr><tr><td><strong>字段</strong></td><td><code>system_prompt_text</code>、<code>prompt_text</code>、<code>completion_text</code>、<code>created_at</code></td></tr></tbody></table><hr><h2 id="_7-长度与截断-业务语义" tabindex="-1">7. 长度与截断（业务语义） <a class="header-anchor" href="#_7-长度与截断-业务语义" aria-label="Permalink to &quot;7. 长度与截断（业务语义）&quot;">​</a></h2><p>配置在 <strong><code>ext/usage_log/config.go</code></strong> 中加载，落库前在 <strong><code>TruncateUTF8</code></strong> 中执行（按 <strong>Unicode 码点 / rune</strong>，不是字节）。</p><table tabindex="0"><thead><tr><th>配置项</th><th>环境变量</th><th>默认</th><th>说明</th></tr></thead><tbody><tr><td>问题侧上限</td><td><code>LOG_EXT_MAX_PROMPT_LEN</code></td><td><strong>2000</strong></td><td><strong>system 提示词</strong>与<strong>用户问题</strong>各自<strong>分别</strong>截断到该长度</td></tr><tr><td>回答上限</td><td><code>LOG_EXT_MAX_COMPLETION_LEN</code></td><td><strong>1000</strong></td><td>合并后的回答截断到该长度</td></tr></tbody></table><p><strong>特殊值：</strong></p><ul><li><strong><code>LOG_EXT_*_LEN=0</code></strong>：与 <code>TruncateUTF8</code> 约定 <strong><code>maxLen &lt;= 0</code> 不截断</strong>（该维度不设上限）。</li><li><strong>未设置、解析失败或负数</strong>：回退到上表默认值（<strong>2000 / 1000</strong>）。</li></ul><hr><h2 id="_8-配置项汇总-环境变量" tabindex="-1">8. 配置项汇总（环境变量） <a class="header-anchor" href="#_8-配置项汇总-环境变量" aria-label="Permalink to &quot;8. 配置项汇总（环境变量）&quot;">​</a></h2><table tabindex="0"><thead><tr><th>环境变量</th><th>默认</th><th>业务含义</th></tr></thead><tbody><tr><td><code>LOG_EXT_RECORD_PROMPT_COMPLETION</code></td><td><code>true</code></td><td><strong>总开关</strong>；<code>false</code> 时<strong>不写</strong>扩展表；但若 <strong><code>LOG_EXT_ATTACHMENT_ENABLED=true</code></strong> 且仍开启 <strong><code>LOG_EXT_ATTACHMENT_INJECT_TO_MESSAGES</code> 或 <code>LOG_EXT_FILE_URL_FETCH_ENABLED</code></strong>，中间件仍会读 body 并做附件注入 / <code>file_url</code> 展开后再转发。</td></tr><tr><td><code>LOG_EXT_MAX_PROMPT_LEN</code></td><td><code>2000</code></td><td>问题侧（system 与 user 各自）最大记录长度（rune）。</td></tr><tr><td><code>LOG_EXT_MAX_COMPLETION_LEN</code></td><td><code>1000</code></td><td>回答最大记录长度（rune）。</td></tr><tr><td><code>LOG_EXT_RECORD_STREAM_COMPLETION</code></td><td>未单独设置时，见下一行</td><td><strong>流式是否写入回答</strong>；<code>false</code> 时流式只记 system/问题。</td></tr><tr><td><code>LOG_EXT_STREAM_ONLY</code></td><td><code>true</code>（在未设置上一项时生效）</td><td><strong>旧名</strong>，与 <strong><code>LOG_EXT_RECORD_STREAM_COMPLETION</code></strong> 同义；若同时设置了 <strong><code>LOG_EXT_RECORD_STREAM_COMPLETION</code></strong>，<strong>以该新变量为准</strong>。</td></tr></tbody></table><p><code>.env</code> 需在主程序加载后再执行 <code>LoadConfig</code>（见 <code>Register</code> 时机），否则进程启动时读不到变量。</p><hr><h2 id="_9-附件-消息内链接-预上传文件" tabindex="-1">9. 附件（消息内链接 + 预上传文件） <a class="header-anchor" href="#_9-附件-消息内链接-预上传文件" aria-label="Permalink to &quot;9. 附件（消息内链接 + 预上传文件）&quot;">​</a></h2><p>在 <strong><code>LOG_EXT_RECORD_PROMPT_COMPLETION=true</code></strong> 且 <strong><code>LOG_EXT_ATTACHMENT_ENABLED=true</code>（默认）</strong> 时生效；附件元数据表 <strong><code>usage_log_attachment</code></strong>，文件体落在 <strong><code>LOG_EXT_ATTACHMENT_ROOT</code></strong>（默认 <code>./data/usage_log_attachments</code>）。</p><h3 id="_9-1-消息内的链接-无需上传" tabindex="-1">9.1 消息内的链接（无需上传） <a class="header-anchor" href="#_9-1-消息内的链接-无需上传" aria-label="Permalink to &quot;9.1 消息内的链接（无需上传）&quot;">​</a></h3><p>中间件在 <strong><code>SaveContent</code> 之后</strong> 解析请求 JSON 的 <strong><code>messages</code></strong>：对 <code>content</code> 为数组时，收集</p><ul><li><code>type: &quot;image_url&quot;</code> → <code>image_url.url</code></li><li><code>type: &quot;file&quot;</code> → <code>file.file_url</code>（若存在）</li></ul><p>同一请求内 <strong>URL 去重</strong> 后写入表，<code>kind=url</code>，<code>source_url</code> 存完整链接；下载接口对 <code>url</code> 类型返回 <strong>302</strong> 到该地址（仅 <code>http</code>/<code>https</code>）。<br><strong>说明</strong>：若启用了下文 <strong>9.1.1</strong> 的 <code>file_url</code> 展开，附件采集仍使用<strong>展开前</strong>的 JSON，因此 <code>file.file_url</code> 仍会写入附件表。</p><h3 id="_9-1-1-file-url-网关侧拉取-无需本地上传" tabindex="-1">9.1.1 <code>file_url</code> 网关侧拉取（无需本地上传） <a class="header-anchor" href="#_9-1-1-file-url-网关侧拉取-无需本地上传" aria-label="Permalink to &quot;9.1.1 \`file_url\` 网关侧拉取（无需本地上传）&quot;">​</a></h3><p>在 <strong><code>LOG_EXT_FILE_URL_FETCH_ENABLED=true</code>（默认）</strong> 且 <strong><code>LOG_EXT_ATTACHMENT_ENABLED=true</code></strong> 时，在<strong>转发给上游 relay 之前</strong>，若 <code>messages[].content</code> 中存在 <strong><code>type: &quot;file&quot;</code></strong>，且 <strong><code>file.file_url</code></strong> 为 <code>http</code>/<code>https</code> <strong>绝对地址</strong>，且<strong>未</strong>设置 <strong><code>file.file_id</code></strong>（或为空），网关会：</p><ol><li><strong>服务端</strong> <code>GET</code> 该 URL（响应体积 ≤ <strong><code>LOG_EXT_ATTACHMENT_MAX_BYTES</code></strong>，并做内网/回环等 <strong>SSRF</strong> 限制）；</li><li>按 <strong><code>Content-Type</code></strong> 与内容：将 <strong>UTF-8 文本</strong>、<strong>HTML</strong>（抽取可见文本）、<strong>JSON</strong> 等转为一段 <strong><code>{&quot;type&quot;:&quot;text&quot;,&quot;text&quot;:&quot;...&quot;}</code></strong>，<strong>替换</strong>原 <code>file</code> 段再交给下游（行为上接近「外链图用 URL」：由网关侧代为取内容，客户端不必先上传文件）；</li><li><strong>PDF</strong>（<code>Content-Type: application/pdf</code> 或 <code>%PDF</code> 魔数）当前<strong>不做</strong>服务端抽字，请求会 <strong>400</strong>，提示改用 <strong>文本/Markdown 公链</strong>或先上传取得 <strong><code>file_id</code></strong>。</li></ol><p>关闭本行为：<strong><code>LOG_EXT_FILE_URL_FETCH_ENABLED=false</code></strong>，则 <code>file_url</code> <strong>不</strong>展开，原样进入 relay（直连要求 <strong><code>file_id</code></strong> 的上游时仍可能报错）。</p><h3 id="_9-2-预上传文件-本地文件" tabindex="-1">9.2 预上传文件（本地文件） <a class="header-anchor" href="#_9-2-预上传文件-本地文件" aria-label="Permalink to &quot;9.2 预上传文件（本地文件）&quot;">​</a></h3><ol><li><p>调用 <strong><code>POST /api/ext/attachment/upload</code></strong>，鉴权与 <strong><code>/v1</code></strong> 一致：<strong><code>TokenOrUserAuth</code></strong> —— <strong>管理端会话</strong> 或 <strong><code>Authorization: Bearer sk-...</code></strong>（与调用 chat 时同一密钥即可）。<code>multipart</code> 支持：</p><ul><li>字段名 <strong><code>files</code></strong>：一次选多个文件；或</li><li>字段名 <strong><code>file</code></strong>：可重复多个 part（与 HTML <code>&lt;input type=&quot;file&quot; multiple name=&quot;file&quot;&gt;</code> 一致）；</li><li>仍兼容<strong>仅一个</strong> <code>file</code> 的旧用法。</li></ul></li><li><p>响应：</p><ul><li><strong><code>data.items</code></strong>：数组，每项含 <strong><code>pending_token</code></strong>、<strong><code>filename</code></strong>、<strong><code>size</code></strong>；</li><li><strong><code>data.pending_tokens</code></strong>：全部 token 的字符串数组；</li><li>若本次<strong>仅 1 个文件</strong>，仍返回 <strong><code>data.pending_token</code> / <code>data.filename</code> / <code>data.size</code></strong>（与旧版一致）。</li></ul></li><li><p>调用 Relay <strong><code>POST /v1/chat/completions</code></strong> 时，将预上传 token 交给网关的方式<strong>二选一</strong>（或同时提供，会去重合并）：</p><ul><li><strong>推荐</strong>：请求头 <strong><code>X-Usage-Log-Attachment-Tokens</code></strong>，值为 <strong>逗号分隔</strong> 的 token，或 JSON 数组字符串 <strong><code>[&quot;t1&quot;,&quot;t2&quot;]</code></strong> —— <strong>无需</strong>在 JSON body 顶层写 <strong><code>usage_log_attachment_tokens</code></strong>；</li><li><strong>兼容</strong>：JSON 顶层 <strong><code>usage_log_attachment_tokens</code></strong>：<code>[&quot;token1&quot;,&quot;token2&quot;]</code>。</li></ul></li><li><p><strong>转发前注入消息（默认）</strong>：在 <strong><code>LOG_EXT_ATTACHMENT_INJECT_TO_MESSAGES=true</code>（默认）</strong> 时，中间件在交给 relay <strong>之前</strong>会根据 token 读取待定文件，向 <strong><code>messages</code> 中最后一条 <code>role=user</code></strong> 的 <strong><code>content</code></strong> 追加 OpenAI 兼容段：</p><ul><li><strong>PDF（默认）</strong>：<strong><code>LOG_EXT_ATTACHMENT_PDF_INJECT_MODE=file</code>（默认）</strong> 时，PDF 以 <strong><code>type:file</code></strong> + <strong><code>file_data: data:application/pdf;base64,...</code></strong> 注入最后一条 user 消息（<strong>不依赖</strong>系统 <strong><code>pdftotext</code></strong>）。<strong>须上游</strong>在 Chat 路径上<strong>支持</strong> <code>messages</code> 内该形态；若上游忽略未知段，模型会看不到 PDF。</li><li><strong><code>LOG_EXT_ATTACHMENT_PDF_INJECT_MODE=text</code></strong> 时，网关调用 <strong><code>pdftotext</code></strong>（需 <strong>poppler-utils</strong>）将 PDF 抽成纯文本，以 <strong><code>type:text</code></strong> 注入——适合<strong>只接受纯文本</strong>、或<strong>不接受</strong> <code>type:file</code> 的上游。</li><li><strong>非 PDF</strong>：仍使用 <strong><code>type:file</code></strong> + Data URL。<br> 须与预上传使用<strong>同一鉴权</strong>（<strong><code>Authorization: Bearer sk-...</code></strong> 或管理端会话），否则返回 <strong>400</strong>。若 <strong><code>LOG_EXT_ATTACHMENT_INJECT_TO_MESSAGES=false</code></strong>，则 token <strong>仅</strong>用于下文第 5 步关联数据库，<strong>不会</strong>把文件塞进 <code>messages</code>（旧行为）。</li></ul></li><li><p>中间件在 <strong><code>c.Next()</code> 之后</strong> 根据 <strong><code>logs.request_id</code></strong>，将待定行中 <strong><code>pending_token</code> 匹配</strong>、且 <strong><code>user_id</code> 等于该日志用户或为 0（历史匿名数据）</strong> 的记录写入 <strong><code>request_id</code></strong>，并把 <strong><code>user_id</code> 更新为日志所属用户</strong>，完成关联。</p></li></ol><p><strong>约束</strong>：每个文件大小 ≤ <strong><code>LOG_EXT_ATTACHMENT_MAX_BYTES</code></strong>（默认 20MiB）；<strong>单次请求最多 20 个文件</strong>。</p><h3 id="_9-2-1-单次请求内联上传-与对话同发-无需先调-api-ext-attachment-upload" tabindex="-1">9.2.1 单次请求内联上传（与对话同发，无需先调 <code>/api/ext/attachment/upload</code>） <a class="header-anchor" href="#_9-2-1-单次请求内联上传-与对话同发-无需先调-api-ext-attachment-upload" aria-label="Permalink to &quot;9.2.1 单次请求内联上传（与对话同发，无需先调 \`/api/ext/attachment/upload\`）&quot;">​</a></h3><p>在 <strong><code>POST /v1/chat/completions</code></strong>（以及 <strong><code>/v1/completions</code></strong>、<strong><code>/v1/messages</code></strong>、<strong><code>/pg/chat/completions</code></strong> 等捕获路径）使用 <strong><code>Content-Type: multipart/form-data</code></strong>：</p><table tabindex="0"><thead><tr><th>表单字段</th><th>说明</th></tr></thead><tbody><tr><td><strong><code>request</code></strong> 或 <strong><code>json</code></strong></td><td><strong>必填</strong>，值为与原先 <strong><code>application/json</code> body 完全相同</strong>的 JSON；可为<strong>纯文本字段</strong>，也可用 <strong><code>curl -F request=@chat.json</code></strong> 上传文件（服务端会从 multipart <strong>文件部件</strong>读取）。</td></tr><tr><td><strong><code>files</code></strong> 或 <strong><code>file</code></strong>（可多个）</td><td>可选，本地文件；网关会写入待定附件并<strong>自动合并</strong>到本次请求的 <strong><code>usage_log_attachment_tokens</code></strong>。</td></tr></tbody></table><p>鉴权需 <strong><code>Authorization: Bearer sk-...</code></strong>（与纯 JSON 聊天相同）或<strong>管理端会话</strong>；<strong>带文件时</strong>必须能解析出用户。</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 1）准备 chat.json（与原来 JSON body 一致，不要写 usage_log_attachment_tokens）</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 2）一次请求带上文件</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -N</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -sS</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;http://127.0.0.1:3000/v1/chat/completions&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Authorization: Bearer YOUR_API_KEY&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;request=@chat.json&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;files=@/path/to/report.pdf&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;files=@/path/to/notes.docx&#39;</span></span></code></pre></div><p>网关内部会把 body 还原为 <strong><code>application/json</code></strong> 再交给下游 relay，行为与「先预上传 + 头里带 token」等价。</p><h3 id="_9-3-配置-附件" tabindex="-1">9.3 配置（附件） <a class="header-anchor" href="#_9-3-配置-附件" aria-label="Permalink to &quot;9.3 配置（附件）&quot;">​</a></h3><table tabindex="0"><thead><tr><th>环境变量</th><th>默认</th><th>说明</th></tr></thead><tbody><tr><td><code>LOG_EXT_ATTACHMENT_ENABLED</code></td><td><code>true</code></td><td>关闭则不上传、不解析链接、不写入附件表。</td></tr><tr><td><code>LOG_EXT_ATTACHMENT_ROOT</code></td><td><code>./data/usage_log_attachments</code></td><td>本地存储根目录（自动 <code>mkdir</code>）。</td></tr><tr><td><code>LOG_EXT_ATTACHMENT_MAX_BYTES</code></td><td><code>20971520</code></td><td>单次上传最大字节数。</td></tr><tr><td><code>LOG_EXT_FILE_URL_FETCH_ENABLED</code></td><td><code>true</code></td><td>是否将无 <code>file_id</code> 的 <strong><code>file.file_url</code></strong> 拉取并替换为 <strong><code>type:text</code></strong>（见 <strong>9.1.1</strong>）。</td></tr><tr><td><code>LOG_EXT_FILE_URL_MAX_TEXT_RUNES</code></td><td><code>200000</code></td><td>单条 URL 展开后的文本最大 <strong>rune</strong> 数；<strong><code>0</code></strong> 表示该条不截断（与 <code>TruncateUTF8</code> 约定一致）。</td></tr><tr><td><code>LOG_EXT_ATTACHMENT_INJECT_TO_MESSAGES</code></td><td><code>true</code></td><td>是否将预上传 token 对应内容注入最后一条 user 消息后再转发（见 <strong>9.2</strong> 第 4 点）。</td></tr><tr><td><code>LOG_EXT_ATTACHMENT_PDF_INJECT_MODE</code></td><td><code>file</code></td><td>PDF：<strong><code>file</code></strong>（默认）= <strong><code>type:file</code></strong> + Data URL，<strong>无需</strong> <code>pdftotext</code>；<strong><code>text</code></strong>= <strong><code>pdftotext</code></strong> 抽字后以 <strong><code>type:text</code></strong> 注入（需安装 poppler-utils）。</td></tr></tbody></table><h3 id="_9-3-1-预上传-pdf-后模型像「没读到文档」" tabindex="-1">9.3.1 预上传 PDF 后模型像「没读到文档」 <a class="header-anchor" href="#_9-3-1-预上传-pdf-后模型像「没读到文档」" aria-label="Permalink to &quot;9.3.1 预上传 PDF 后模型像「没读到文档」&quot;">​</a></h3><ul><li><strong>现象</strong>：<code>upload</code> 成功、请求头已带 <code>X-Usage-Log-Attachment-Tokens</code>，回复却与 PDF 无关。</li><li><strong>常见原因（默认 <code>file</code> 直传）</strong>：下游 <strong>Chat Completions</strong> 实现<strong>未实现</strong>或<strong>丢弃</strong> <code>messages</code> 里的 <strong><code>type:file</code></strong>，模型只看到文字指令。</li><li><strong>处理</strong>：换用<strong>支持</strong>该字段的<strong>渠道/上游</strong>；或改为 <strong><code>LOG_EXT_ATTACHMENT_PDF_INJECT_MODE=text</code></strong> 并在网关机安装 <strong>poppler-utils</strong>（将 PDF 抽成 <strong><code>type:text</code></strong>，兼容性更好，但依赖系统命令）。</li></ul><h3 id="_9-4-下载与权限" tabindex="-1">9.4 下载与权限 <a class="header-anchor" href="#_9-4-下载与权限" aria-label="Permalink to &quot;9.4 下载与权限&quot;">​</a></h3><ul><li><strong><code>GET /api/ext/attachment/download?id=&lt;附件主键&gt;</code></strong>（需登录）：校验附件的 <strong><code>request_id</code></strong> 对应 <strong><code>logs</code></strong> 归属当前用户或管理员。</li><li><strong><code>kind=upload</code></strong>：读磁盘文件并 <strong><code>Content-Disposition</code> 下载</strong>。</li><li><strong><code>kind=url</code></strong>：<strong>302</strong> 重定向到 <code>source_url</code>。</li></ul><h3 id="_9-5-联调示例-curl-预上传-流式聊天" tabindex="-1">9.5 联调示例（curl：预上传 + 流式聊天） <a class="header-anchor" href="#_9-5-联调示例-curl-预上传-流式聊天" aria-label="Permalink to &quot;9.5 联调示例（curl：预上传 + 流式聊天）&quot;">​</a></h3><p><strong>本机上的 PDF / Word / Excel 等</strong>：OpenAI 风格 <strong><code>messages[].content</code></strong> 里<strong>没有</strong>「直接塞本地文件二进制」的标准字段；网关侧 <strong><code>file.file_url</code></strong> 只解析 <strong>http(s) 外链</strong>。因此 <strong>本地文档</strong> 须：</p><ol><li><strong><code>POST /api/ext/attachment/upload</code></strong> 用 <strong><code>multipart</code></strong> 上传（可多文件）；</li><li>聊天请求用请求头 <strong><code>X-Usage-Log-Attachment-Tokens</code></strong> 带上返回的 <strong><code>pending_token</code></strong>（与 <strong><code>Authorization: Bearer sk-...</code></strong> 同一密钥），才会记入 **<code>usage_log_attachment**</code> 并与本次 <strong><code>request_id</code></strong> 关联。</li></ol><p><strong>与外链、内嵌图的区别</strong>（摘要）：</p><table tabindex="0"><thead><tr><th>内容</th><th>怎么交给网关 / 日志</th></tr></thead><tbody><tr><td>外链图片 / 外链 PDF</td><td><code>image_url.url</code> 或 <code>file.file_url</code></td></tr><tr><td>本地小图（给模型看）</td><td>常用 <strong><code>data:image/...;base64,...</code></strong> 写在 <code>image_url.url</code></td></tr><tr><td><strong>本机 pdf / doc(x) / xlsx 等</strong></td><td><strong>预上传</strong> + <strong><code>X-Usage-Log-Attachment-Tokens</code></strong>（<strong>不能</strong>只靠 JSON 里一段 base64 文件名）</td></tr></tbody></table><p><strong>1）预上传本地文件</strong>（与 chat 使用<strong>同一 <strong><code>Authorization: Bearer sk-...</code></strong> 密钥</strong>）。示例同时上传图片与多种办公文档：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -sS</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;http://127.0.0.1:3000/api/ext/attachment/upload&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Authorization: Bearer YOUR_API_KEY&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;files=@/path/to/photo.png&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;files=@/path/to/report.pdf&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;files=@/path/to/notes.docx&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;files=@/path/to/data.xlsx&#39;</span></span></code></pre></div><p>从响应 <strong><code>data.pending_tokens</code></strong>（或 <strong><code>data.items[].pending_token</code></strong>）复制全部 token。</p><p><strong>2）调用聊天接口</strong>：在 <strong><code>X-Usage-Log-Attachment-Tokens</code></strong> 中填入上一步 token（逗号分隔）；<strong><code>messages</code></strong> 里仍可写外链图、外链 PDF、base64 图等。</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -N</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -sS</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;http://127.0.0.1:3000/v1/chat/completions&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Authorization: Bearer YOUR_API_KEY&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;X-Usage-Log-Attachment-Tokens: TOKEN_FROM_PNG,TOKEN_FROM_PDF,TOKEN_FROM_DOCX,TOKEN_FROM_XLSX&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Content-Type: application/json&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @-</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;EOF&#39;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">{</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;model&quot;: &quot;gpt-4o&quot;,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;stream&quot;: true,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;messages&quot;: [</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;role&quot;: &quot;system&quot;,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;content&quot;: &quot;你是专业助手，回答简洁。&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    },</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;role&quot;: &quot;user&quot;,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;content&quot;: [</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">          &quot;type&quot;: &quot;text&quot;,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">          &quot;text&quot;: &quot;请结合：外链图、外链 PDF、base64 本地小图；本机的 pdf/docx/xlsx 已通过预上传并在请求头关联 token。&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        },</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">          &quot;type&quot;: &quot;image_url&quot;,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">          &quot;image_url&quot;: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">            &quot;url&quot;: &quot;https://img-s.msn.cn/tenant/amp/entityid/AA1Z6SrD.img?w=600&amp;h=466&amp;m=6&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">          }</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        },</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">          &quot;type&quot;: &quot;image_url&quot;,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">          &quot;image_url&quot;: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">            &quot;url&quot;: &quot;data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8z8BQDwAEhQGAhKmMIQAAAABJRU5ErkJggg==&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">          }</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        }</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      ]</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    }</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  ]</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">EOF</span></span></code></pre></div><p>说明：<strong>OpenAI 官方</strong>等上游若要在 <code>content</code> 里带文档，通常要求 <strong><code>file.file_id</code></strong>（先走 Files API），<strong>不会</strong>把任意 <strong><code>file.file_url</code></strong> 当可下载文档交给模型。若开启 <strong>9.1.1</strong>（默认），本网关会先把 <strong><code>file_url</code>（且无 <code>file_id</code>）</strong> 拉成 <strong>纯文本</strong>再转发，则下游收到的是 <strong><code>type:text</code></strong>，一般不再出现「缺少 <code>file_id</code>」。若关闭 <strong><code>LOG_EXT_FILE_URL_FETCH_ENABLED</code></strong>，则 <strong><code>file_url</code></strong> 仍可能原样进入上游，此时仅便于 <strong>usage_log</strong> 从消息里收集 URL 记库，联调直连 OpenAI 时可能报 <code>missing_required_parameter</code>。<strong>模型是否真的能「读」本地 pdf/doc</strong> 仍取决于渠道；<strong>usage_log</strong> 侧 token 关联成功即可在管理端看到附件。</p><p>（若仍使用 JSON 顶层的 <strong><code>usage_log_attachment_tokens</code></strong>，可与请求头<strong>合并</strong>，重复 token 会去重。）</p><p><a id="sec-excelize"></a></p><h3 id="_9-6-主仓库-go-依赖-github-com-xuri-excelize-v2" tabindex="-1">9.6 主仓库 Go 依赖：<code>github.com/xuri/excelize/v2</code> <a class="header-anchor" href="#_9-6-主仓库-go-依赖-github-com-xuri-excelize-v2" aria-label="Permalink to &quot;9.6 主仓库 Go 依赖：\`github.com/xuri/excelize/v2\`&quot;">​</a></h3><p><strong>作用（业务）</strong><br> 预上传或内联上传的 <strong><code>.xlsx</code> / <code>.xlsm</code></strong>（OpenXML 电子表格）在需要<strong>注入到 <code>messages</code></strong> 时，扩展不会把整个二进制当 PDF/图片处理，而是由 <strong><code>ext/usage_log/attachment_xlsx.go</code></strong> 调用 <strong>excelize</strong> 打开工作簿，按工作表读出单元格，拼成<strong>分段纯文本</strong>（制表符分列、多表带标题、并限制单表行数/工作表数量以防爆内存），再按与其它办公附件类似的逻辑转为可注入内容（例如以 <strong><code>type:text</code></strong> 等形式参与转发，具体与同路径下 <code>attachment_process.go</code> 等一致）。</p><p><strong>作用（工程）</strong><br> 该文件 <strong><code>import github.com/xuri/excelize/v2</code></strong>，因此<strong>主仓库</strong>的 <strong><code>go.mod</code> / <code>go.sum</code></strong> 必须包含该模块（及 <code>go mod tidy</code> 写入的间接依赖）。否则 <strong><code>go build</code> 报错</strong>：<code>no required module provides package github.com/xuri/excelize/v2</code>。</p><p><strong>是否「整个日志插件都必须」</strong></p><ul><li><strong>编译当前源码</strong>：<strong>必须</strong>保留该依赖，否则无法完成构建。</li><li><strong>功能上</strong>：仅在与 <strong>Excel 类附件</strong>的解析/注入路径相关；不涉及 PDF、纯链接、图片等其它附件逻辑。若将来删除 <code>attachment_xlsx.go</code> 及相关调用链，可再通过 <code>go mod tidy</code> 尝试移除 excelize（本文不展开）。</li></ul><p><strong>合并上游后</strong>若依赖丢失，补全方式见 <a href="./usage_log与上游合并-源码入侵.html">usage_log与上游合并-源码入侵.md</a> 中的 <strong><code>go.mod</code> / <code>go.sum</code></strong> 一节。</p><hr><h2 id="_10-只读-api" tabindex="-1">10. 只读 API <a class="header-anchor" href="#_10-只读-api" aria-label="Permalink to &quot;10. 只读 API&quot;">​</a></h2><table tabindex="0"><thead><tr><th>方法</th><th>路径</th><th>说明</th></tr></thead><tbody><tr><td>GET</td><td><code>/api/ext/log-content?request_id=xxx</code></td><td>需登录；校验 <code>request_id</code>；返回 <code>system_prompt_text</code>、<code>prompt_text</code>、<code>completion_text</code>、<strong><code>attachments</code></strong> 列表（含 <code>download_url</code>）。无文本记录时仍可能返回仅 <strong><code>attachments</code></strong>。</td></tr><tr><td>POST</td><td><code>/api/ext/attachment/upload</code></td><td>预上传，见第 9.2 节。</td></tr><tr><td>GET</td><td><code>/api/ext/attachment/download?id=</code></td><td>下载或跳转，见第 9.4 节。</td></tr></tbody></table><hr><h2 id="_11-前端-可选扩展" tabindex="-1">11. 前端（可选扩展） <a class="header-anchor" href="#_11-前端-可选扩展" aria-label="Permalink to &quot;11. 前端（可选扩展）&quot;">​</a></h2><p>管理端「使用日志」通过 <strong><code>web/src/ext/usage-logs/</code></strong> 展示「问题 / 对话详情」列；详情内为 <strong>提示词、问题（正文与关联附件同属提问区）、回答</strong>。<strong><code>VITE_EXT_USAGE_LOG_CONTENT=false</code></strong> 可关闭扩展表。</p><p><strong>入口替换、<code>locales</code> 所需 key</strong> 见 <a href="./usage_log与上游合并-源码入侵.html#frontend-web">usage_log与上游合并-源码入侵 · 前端</a>；组件职责见 <a href="./插件开发.html#sec-2-7-5">插件开发 · 第 2.7.5 节</a>。</p><hr><h2 id="_12-限制与已知情况" tabindex="-1">12. 限制与已知情况 <a class="header-anchor" href="#_12-限制与已知情况" aria-label="Permalink to &quot;12. 限制与已知情况&quot;">​</a></h2><ul><li><strong>流式</strong>：依赖完整 SSE 缓冲；客户端中断、网关超时可能导致回答<strong>不完整</strong>。</li><li><strong>超长</strong>：受 <strong><code>LOG_EXT_MAX_*</code></strong> 截断。</li><li><strong>路径</strong>：仅覆盖第 4.1 节所列；未列出的 relay 路径<strong>不会</strong>写入扩展表。</li><li><strong>附件</strong>：依赖 <strong><code>logs</code> 已写入</strong> 后才能按 <code>user_id</code> 关联预上传令牌；若极端情况下日志滞后，关联可能失败（见服务端日志）。</li></ul><hr><h2 id="_13-源码索引" tabindex="-1">13. 源码索引 <a class="header-anchor" href="#_13-源码索引" aria-label="Permalink to &quot;13. 源码索引&quot;">​</a></h2><table tabindex="0"><thead><tr><th>模块</th><th>路径</th></tr></thead><tbody><tr><td>配置与加载</td><td><code>ext/usage_log/config.go</code></td></tr><tr><td>捕获中间件与解析</td><td><code>ext/usage_log/middleware.go</code>、<code>file_url_expand.go</code>、<code>attachment_inject.go</code></td></tr><tr><td>落库</td><td><code>ext/usage_log/store.go</code></td></tr><tr><td>附件存储与解析</td><td><code>ext/usage_log/attachment_store.go</code>、<code>attachment_parse.go</code>、<code>attachment_process.go</code>、<code>attachment_handler.go</code></td></tr><tr><td><strong>表格（xlsx/xlsm）纯文本抽取</strong></td><td><code>ext/usage_log/attachment_xlsx.go</code>（依赖主仓 <strong><code>go.mod</code></strong> 中的 <strong><code>github.com/xuri/excelize/v2</code></strong>，见 <strong>第 9.6 节</strong>）</td></tr><tr><td>模型与迁移</td><td><code>ext/usage_log/model_ext.go</code></td></tr><tr><td>路由</td><td><code>ext/usage_log/router_ext.go</code></td></tr><tr><td>Context 键</td><td><code>ext/usage_log/keys.go</code></td></tr><tr><td>注册</td><td><code>ext/usage_log/register.go</code></td></tr><tr><td>Relay 单点接入</td><td><code>ext/relay.go</code></td></tr><tr><td>表初始化脚本</td><td><code>ext/table_init/usage_log_attachment_*.sql</code></td></tr><tr><td><strong>主仓库侵入点（合并上游用）</strong></td><td><a href="./usage_log与上游合并-源码入侵.html">usage_log与上游合并-源码入侵.md</a>（仅非 <code>ext/</code> 源码）</td></tr></tbody></table><hr><h2 id="_14-四份文档怎样配合" tabindex="-1">14. 四份文档怎样配合 <a class="header-anchor" href="#_14-四份文档怎样配合" aria-label="Permalink to &quot;14. 四份文档怎样配合&quot;">​</a></h2><table tabindex="0"><thead><tr><th>文档</th><th>职责</th></tr></thead><tbody><tr><td><strong>本文（日志插件）</strong></td><td><strong>业务</strong>：目标、流程、环境变量、附件、API、前端语义、限制；<strong>Excel / excelize</strong> 见 <strong>第 9.6 节</strong>。</td></tr><tr><td><strong><a href="./插件开发.html">插件开发.md</a></strong></td><td><strong>机制</strong>：<code>ext</code> 包、目录、新建插件、表初始化、源码摘录、附录；业务数字以<strong>本文</strong>为准。</td></tr><tr><td><strong><a href="./usage_log与上游合并-源码入侵.html">usage_log与上游合并-源码入侵.md</a></strong></td><td><strong>合并</strong>：主仓库须保留的 <strong>Go / 前端 / i18n / go.mod</strong>；<strong>〔合并段〕</strong> 与源码注释 <code>// 开始</code> 对应。</td></tr><tr><td><strong><a href="./与上游-Gitee-手动合并步骤.html">与上游-Gitee-手动合并步骤.md</a></strong></td><td><strong>Git</strong>：仅当与 Gitee <strong>无共同祖先</strong>时的 <code>merge</code>、<code>stash</code>、<code>-X theirs</code>；<strong>不包含</strong>业务与侵入代码块。</td></tr></tbody></table><p><strong>典型路径：</strong> 日常查配置 → <strong>本文</strong>；写 <code>ext/</code> 或联调 → <strong>插件开发</strong>；拉上游代码 → <strong>Gitee 步骤</strong>（若需要）→ <strong>源码侵入</strong> 逐项恢复 → <code>go build</code>。</p>`,115)])])}const u=o(d,[["render",r]]);export{p as __pageData,u as default};
