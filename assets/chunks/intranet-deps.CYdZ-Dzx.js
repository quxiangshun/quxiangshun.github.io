const n='# 内网依赖配置\n\n> 编辑日期：2026-03-16\n\n本文适用于**禁止外网访问**的环境：使用公司 Artifactory 作为 Go、npm、yarn 依赖源，**不回源公网**。以下地址为公司提供的仓库，直接使用即可。\n\n---\n\n## 一、公司提供的仓库地址\n\n| 类型 | 地址 | 用途 |\n|------|------|------|\n| **Go** | `http://artifactory.ly/go_official_remote/` | 后端 `go.mod` 依赖 |\n| **npm** | `http://artifactory.ly/npm_remote/` | 前端 `web/` 的 npm/Bun 依赖 |\n| **yarn** | `http://artifactory.ly/yarn_remote/` | 使用 yarn 时的依赖源 |\n\n---\n\n## 二、Go 依赖（后端）\n\n在**执行 `go mod download` / `go build` 的终端或 CI** 中设置环境变量，**不要加 `,direct`**（否则会尝试走公网）。\n\n```bash\nexport GOPROXY=http://artifactory.ly/go_official_remote/\n\n# 若需认证（按公司要求）\n# export GOPROXY=http://用户名:密码@artifactory.ly/go_official_remote/\n\n# 若内网仓库不提供 sum 校验，可关闭\nexport GONOSUMDB="*"\n```\n\n然后执行：\n\n```bash\ngo mod download\n# 或\ngo build ./...\n```\n\n**在哪里配置：**\n\n| 场景 | 配置方式 |\n|------|----------|\n| 当前终端临时 | 直接执行上述 `export` 再执行 `go` 命令。 |\n| 本机长期 | 写入 `~/.bashrc` 或 `~/.zshrc`：`echo \'export GOPROXY=http://artifactory.ly/go_official_remote/\' >> ~/.bashrc`，然后 `source ~/.bashrc`。 |\n| 项目/CI 统一 | 在项目脚本（如 `scripts/go-env.sh`）中写 `export GOPROXY=...`，执行 `go` 前先 `source` 该脚本；CI 中在运行 `go` 的步骤前设置同名环境变量。 |\n\n---\n\n## 三、前端依赖（npm / Bun / yarn）\n\n前端在 **`web/`** 目录下安装依赖。本项目推荐使用 **Bun**，若使用 **yarn** 或 **npm** 同样可指向公司仓库。\n\n### 3.1 使用 Bun（项目推荐）\n\n在 **`web/`** 下新建或修改 `.npmrc`：\n\n```ini\nregistry=http://artifactory.ly/npm_remote/\n```\n\n若仓库需要认证，增加（按公司提供的方式二选一）：\n\n```ini\n# 方式一：Token（Base64(用户名:密码)）\n# echo -n "用户名:密码" | base64 得到后填下面\n# //artifactory.ly/npm_remote/:_authToken=BASE64字符串\n\n# 方式二：用户名 + 密码\n# //artifactory.ly/npm_remote/:username=你的用户名\n# //artifactory.ly/npm_remote/:_password=BASE64编码的密码\n```\n\n然后执行：\n\n```bash\ncd web && bun install\n```\n\n### 3.2 使用 npm\n\n```bash\ncd web\nnpm config set registry http://artifactory.ly/npm_remote/\nnpm install\n```\n\n或在该目录下 `.npmrc` 中写：\n\n```ini\nregistry=http://artifactory.ly/npm_remote/\n```\n\n### 3.3 使用 yarn\n\n```bash\ncd web\nyarn config set registry http://artifactory.ly/yarn_remote/\nyarn install\n```\n\n或在该目录下 `.yarnrc` 中写：\n\n```\nregistry "http://artifactory.ly/yarn_remote/"\n```\n\n---\n\n## 四、快速对照\n\n| 类型 | 环境变量 / 配置 | 值 |\n|------|-----------------|-----|\n| Go | `GOPROXY` | `http://artifactory.ly/go_official_remote/` |\n| Go | `GONOSUMDB`（可选） | `*`（内网无 sum 时使用） |\n| npm/Bun | `web/.npmrc` 的 `registry` | `http://artifactory.ly/npm_remote/` |\n| yarn | `web/.yarnrc` 的 `registry` 或 `yarn config set registry` | `http://artifactory.ly/yarn_remote/` |\n\n依赖缺失时需联系管理员在公司 Artifactory 侧补齐或同步，本地不访问公网。\n';export{n as default};
