const o="---\ntitle: 离线镜像与命名（localhost/pgsql）\ndescription: 使用 docker/podman save、load 得到与容器脚本一致的 localhost/pgsql:版本 标签\n---\n\n# 离线镜像与命名（`localhost/pgsql`）\n\n容器脚本里使用的镜像是 **`localhost/pgsql:16.4`**、**`localhost/pgsql:17.9`**。在 `podman images` / `docker images` 中期望看到类似：\n\n| REPOSITORY       | TAG   | IMAGE ID     | SIZE   |\n| ---------------- | ----- | ------------ | ------ |\n| localhost/pgsql  | 16.4  | 6b14e73a48cf | 439 MB |\n| localhost/pgsql  | 17.9  | f7b934223918 | 461 MB |\n\n若你用 **`docker save -o postgres-17.9.tar postgres:17.9`** 导出，离线机 **`load`** 后镜像名往往是官方默认名（如 **`postgres`**、**`docker.io/library/postgres`**），**不会**自动变成 `localhost/pgsql:17.9`。若曾错误地把镜像标成 **`localhost/17.9:latest`**，也需要改成本文约定名，才能与 [16.4](./docker-pg16.4)、[17.9](./docker-pg17.9) 脚本一致。\n\n下面命令中 **`podman`** 与 **`docker`** 可互换（二者 `save`/`load`/`tag` 行为一致）。\n\n---\n\n## 一、推荐：联网机先改名再打包（离线机 load 即正确）\n\n在能拉取镜像的机器上，先打成 **`localhost/pgsql:<版本>`**，再导出，离线机只需 `load`，无需再 `tag`。\n\n```bash\n# 示例：PostgreSQL 17.9\npodman pull docker.io/library/postgres:17.9\npodman tag postgres:17.9 localhost/pgsql:17.9\npodman save -o postgres-17.9.tar localhost/pgsql:17.9\n```\n\n离线机：\n\n```bash\npodman load -i postgres-17.9.tar\npodman images | grep localhost/pgsql\n# 应看到：localhost/pgsql   17.9   ...\n```\n\n16.4 同理（版本号换成你的镜像标签，如 `16.4`）：\n\n```bash\npodman pull docker.io/library/postgres:16.4\npodman tag postgres:16.4 localhost/pgsql:16.4\npodman save -o postgres-16.4.tar localhost/pgsql:16.4\n```\n\n---\n\n## 二、已有 `postgres-17.9.tar`（保存的是 `postgres:17.9`）\n\n离线机先 `load`，再 **增加** 一条本项目用的标签（不覆盖原标签也可，运行脚本只需 `localhost/pgsql:17.9` 存在即可）。\n\n```bash\npodman load -i postgres-17.9.tar\n# 查看实际名称（可能是 postgres、docker.io/library/postgres 等）\npodman images\n\npodman tag postgres:17.9 localhost/pgsql:17.9\n# 若上一步报错，用下面之一（按 images 列表里的 REPOSITORY 为准）：\n# podman tag docker.io/library/postgres:17.9 localhost/pgsql:17.9\n```\n\n可选：删除多余标签，避免混淆：\n\n```bash\npodman rmi postgres:17.9\n# 仅在本机已存在 localhost/pgsql:17.9 指向同一镜像时安全；否则先不要删\n```\n\n---\n\n## 三、已变成 `localhost/17.9   latest` 时如何改\n\n这通常是对镜像名理解偏差导致的 **错误仓库名**（把 `17.9` 当成了仓库名的一部分）。直接 **再 tag 一次** 即可：\n\n```bash\npodman tag localhost/17.9:latest localhost/pgsql:17.9\npodman rmi localhost/17.9:latest\n```\n\n若 `rmi` 提示被容器占用，可先停删容器再删标签，或保留 `localhost/17.9` 仅使用 `localhost/pgsql:17.9` 运行新容器。\n\n---\n\n## 四、核对\n\n```bash\npodman images --format 'table {{.Repository}}\\t{{.Tag}}\\t{{.ID}}\\t{{.Size}}' | grep -E 'REPOSITORY|pgsql'\n```\n\n应能看到 **`localhost/pgsql`** 与 **`16.4` / `17.9`** 等 TAG，再执行各版本的 `docker-pg*.sh` 即可。\n";export{o as default};
