const n='---\ntitle: PostgreSQL 17.9 容器安装（Podman）\ndescription: 使用 Podman 运行 PostgreSQL 17.9，挂载配置与数据目录\n---\n\n# PostgreSQL 17.9 容器安装（Podman）\n\n本文提供在 Linux 上使用 **Podman** 启动 PostgreSQL 17.9 的示例脚本（镜像为 `localhost/pgsql:17.9`，需事先构建或导入）。与 [16.4 安装](./docker-pg16.4) 并列，目录与容器名独立，可与 16.4 **同时运行**（见下文端口说明）。离线包导入后若显示为 `postgres:17.9` 或 `localhost/17.9`，见 [离线镜像与命名](./offline-image)。\n\n## 与 16.4 并存\n\n- **数据目录**：使用 `/opt/pgsql179/...`，与 16.4 的 `/opt/pgsql164/...` 完全分离。\n- **容器名**：`pgsql179`，与 `pgsql164` 不冲突。\n- **宿主机端口**：脚本将容器内 `5432` 映射为宿主机 **`5433`**，避免与 16.4 占用的 **`5432`** 冲突。连接示例：`psql -h 127.0.0.1 -p 5433 -U postgres`。\n\n若本机只跑 17.9、不需要与 16.4 同机并存，可将脚本中的 `5433:5432` 改为 `5432:5432`。\n\n## 本地尚无配置文件时\n\n宿主机还没有 **`/opt/pgsql179/conf/postgresql.conf`**、**`pg_hba.conf`** 时，不要先挂空文件或错误路径。做法是：**先只挂数据目录**（不挂配置），让镜像完成 `initdb`，再把容器里 **`$PGDATA` 下默认生成的两个文件**拷到宿主机，最后停掉临时容器，再执行正式脚本 `docker-pg17.9.sh`（此时数据目录里已有集群，会直接启动）。\n\n### 方式一：脚本（推荐）\n\n仓库内：`tech-share/pgsql/bootstrap-conf.sh`（支持 `16.4` / `17.9`）。\n\n```bash\nchmod +x bootstrap-conf.sh\nsudo mkdir -p /opt/pgsql179/conf /opt/pgsql179/data /opt/pgsql179/logs\n# 确保 data 完全为空：`ls -la /opt/pgsql179/data` 应只有 . 与 ..\n# 若有残留（含隐藏文件），先 podman rm -f pgsql179-init，再：\n#   sudo find /opt/pgsql179/data -mindepth 1 -delete\n./bootstrap-conf.sh 17.9\n./docker-pg17.9.sh\n```\n\n`PG_IMAGE` 可覆盖镜像，例如：`PG_IMAGE=localhost/pgsql:17.9 ./bootstrap-conf.sh 17.9`。\n\n### 方式二：手动命令\n\n```bash\nsudo mkdir -p /opt/pgsql179/conf /opt/pgsql179/data /opt/pgsql179/logs\n# /opt/pgsql179/data 必须完全为空（含隐藏文件）。先检查：\nls -la /opt/pgsql179/data\n# 若仅有 . 与 .. 可继续；若有其它文件，先停容器再清空（无备份勿删）：\n#   podman rm -f pgsql179-init\n#   sudo find /opt/pgsql179/data -mindepth 1 -delete\n\npodman run -d --name pgsql179-init \\\n  -e TZ=Asia/Shanghai \\\n  -e POSTGRES_PASSWORD=123456 \\\n  -e POSTGRES_USER=postgres \\\n  -e POSTGRES_DB=postgres \\\n  -v /opt/pgsql179/data:/var/lib/postgresql/data:Z \\\n  localhost/pgsql:17.9\n\n# 等待就绪后复制默认配置\nuntil podman exec pgsql179-init pg_isready -U postgres; do sleep 1; done\npodman cp pgsql179-init:/var/lib/postgresql/data/postgresql.conf /opt/pgsql179/conf/postgresql.conf\npodman cp pgsql179-init:/var/lib/postgresql/data/pg_hba.conf /opt/pgsql179/conf/pg_hba.conf\n\npodman stop pgsql179-init && podman rm pgsql179-init\n\n./docker-pg17.9.sh\n```\n\n## 前置说明\n\n- 宿主机需准备目录与配置文件，例如：\n  - `/opt/pgsql179/conf/postgresql.conf`\n  - `/opt/pgsql179/conf/pg_hba.conf`\n  - `/opt/pgsql179/data`（**首次初始化前必须为空目录**；若尚无 conf，先按上文「本地尚无配置文件时」生成）\n  - `/opt/pgsql179/logs`（日志目录）\n- **配置文件不要挂进数据目录**：`postgresql.conf` / `pg_hba.conf` 挂载到容器内 **`/etc/postgresql/`**，数据目录只挂 **`/var/lib/postgresql/data`**。若把两个 conf 挂到 `$PGDATA` 里，镜像在首次启动时执行 `initdb` 会认为数据目录「非空」而失败（见下方「常见错误」）。\n- 卷挂载使用 `:Z` 标签，适用于 rootless Podman 的 SELinux 场景。\n- 若使用 **Docker**，可将命令中的 `podman` 换为 `docker`，并视环境去掉或调整 `:Z`。\n\n## 脚本\n\n仓库内脚本路径：`tech-share/pgsql/docker-pg17.9.sh`。\n\n```bash\n#!/bin/bash\npodman run -d \\\n  --name pgsql179 \\\n  --restart=always \\\n  --memory=1G \\\n  --cpus=1 \\\n  -p 0.0.0.0:5433:5432 \\\n  -e TZ=Asia/Shanghai \\\n  -e POSTGRES_PASSWORD=123456 \\\n  -e POSTGRES_USER=postgres \\\n  -e POSTGRES_DB=postgres \\\n  -v /opt/pgsql179/conf/postgresql.conf:/etc/postgresql/postgresql.conf:Z \\\n  -v /opt/pgsql179/conf/pg_hba.conf:/etc/postgresql/pg_hba.conf:Z \\\n  -v /opt/pgsql179/data:/var/lib/postgresql/data:Z \\\n  -v /opt/pgsql179/logs:/var/log/postgresql:Z \\\n  localhost/pgsql:17.9 \\\n  postgres -c config_file=/etc/postgresql/postgresql.conf -c hba_file=/etc/postgresql/pg_hba.conf\n```\n\n## 使用\n\n```bash\nchmod +x docker-pg17.9.sh\n./docker-pg17.9.sh\n```\n\n> **安全提示**：示例中的 `POSTGRES_PASSWORD` 仅为演示，生产环境请使用强密码并通过密钥或编排工具注入。\n\n## 配置文件权限（宿主机） {#conf-perms}\n\n容器内 **`postgres`** 进程（官方镜像一般为 **UID 999**）必须能**读**挂载的 `postgresql.conf`、`pg_hba.conf`。若用 **`podman cp`** 或 root 编辑后文件为 **`600` 且属主 root**，会出现：\n\n`could not open configuration file "/etc/postgresql/postgresql.conf": Permission denied`\n\n在宿主机执行（路径按实际）：\n\n```bash\nchmod 644 /opt/pgsql179/conf/postgresql.conf /opt/pgsql179/conf/pg_hba.conf\n```\n\n若仍异常，与容器内用户对齐（先查 UID：`podman run --rm localhost/pgsql:17.9 id -u postgres`）：\n\n```bash\nchown 999:999 /opt/pgsql179/conf/postgresql.conf /opt/pgsql179/conf/pg_hba.conf\n```\n\n挂载的 **日志目录** `/opt/pgsql179/logs` 若需容器内写文件，需对 **postgres** 可写，例如：\n\n```bash\nchown 999:999 /opt/pgsql179/logs\n# 或 chmod 1777 /opt/pgsql179/logs（仅测试）\n```\n\n## 常见错误\n\n### `initdb: directory "/var/lib/postgresql/data" exists but is not empty`\n\n- **原因**：首次初始化前，`$PGDATA` 里已有文件。常见情况是把 `postgresql.conf` / `pg_hba.conf` 挂到了 **`/var/lib/postgresql/data/`** 下，或宿主机 `/opt/pgsql179/data` 里已有残留文件。\n- **处理**：使用当前脚本（配置挂 **`/etc/postgresql/`**，数据目录单独挂卷）；清空宿主机数据目录后重试（**会丢失该目录下未备份数据**）：\n\n```bash\nsudo rm -rf /opt/pgsql179/data/*\n# 确认无容器占用后再执行；必要时先 podman rm -f pgsql179\n```\n\n- 若自建的 `postgresql.conf` 里仍写死了 `hba_file = \'.../data/pg_hba.conf\'`，请改为与挂载一致（例如 `\'/etc/postgresql/pg_hba.conf\'`），或删掉该行，由启动参数 `-c hba_file=...` 生效。\n\n### `Emulate Docker CLI using podman`\n\n此为 Podman 兼容提示，不影响运行。若需静默，可在 root 下执行：`mkdir -p /etc/containers && touch /etc/containers/nodocker`（见 Podman 文档）。\n\n### 仅挂数据目录仍报 `initdb: ... is not empty`（bootstrap / `pgsql179-init`）\n\n说明宿主机 **`/opt/pgsql179/data` 里已有任意条目**（不限于可见文件，也可能是上次失败留下的 `PG_VERSION`、目录等）。**`mkdir -p` 不会清空已有内容**。\n\n1. 删除失败容器：`podman rm -f pgsql179-init`\n2. 确认目录内容：`ls -la /opt/pgsql179/data`\n3. 在确认无重要数据后**彻底清空**（慎用）：\n\n```bash\nsudo find /opt/pgsql179/data -mindepth 1 -delete\n```\n\n4. 再执行 `podman run ...` 或 `./bootstrap-conf.sh 17.9`。\n\n### `could not open configuration file "/etc/postgresql/postgresql.conf": Permission denied`\n\n见上文 **[配置文件权限（宿主机）](#conf-perms)**，一般为 **`chmod 644`** 两个 conf 即可。\n';export{n as default};
